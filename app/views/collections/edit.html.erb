<h1>Editing: <%= @user.username %> / <%= @collection.title %></h1>

<%= render 'form' %>

<%= link_to 'Show', user_collection_path(@collection.user, @collection) %> |
<%= link_to 'Back', user_collections_path %>
